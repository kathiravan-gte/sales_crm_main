# ── CRM Platform — Environment Variables ────────────────────────────────────
# Copy this file to .env and fill in real values.
# .env is git-ignored; this file is safe to commit.

# ── Core ─────────────────────────────────────────────────────────────────────
SECRET_KEY=change-me-in-production
DEMO_MODE=True

# ── Email (SMTP) ──────────────────────────────────────────────────────────────
# Leave blank to disable email sending.
# Gmail example: SMTP_SERVER=smtp.gmail.com, SMTP_PORT=587
# Use an App Password if 2FA is enabled on the account.
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_NAME=CRM Platform

# ── WhatsApp via Twilio ───────────────────────────────────────────────────────
# Leave blank to disable WhatsApp sending.
# 1. Sign up at https://www.twilio.com
# 2. Enable the WhatsApp Sandbox (or a production number)
# 3. Find SID + Auth Token in the Twilio Console
# TWILIO_WHATSAPP_NUMBER format: "whatsapp:+14155238886"
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# ── AI Assistant (Anthropic / Claude) ────────────────────────────────────────
# Leave blank to run in keyword-only mode (no LLM, fully functional).
# Get your API key at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx
# Model to use — haiku is fast and cheap; swap for sonnet/opus for richer answers
LLM_MODEL=claude-haiku-4-5-20251001
